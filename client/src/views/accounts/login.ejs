<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <title>Login</title>
    <style>
        body {
            background: #fff6f6;
            font-family: Arial, sans-serif;
            margin: 0;
            padding: 0;
        }
        .login-container {
            max-width: 350px;
            margin: 80px auto;
            padding: 30px 25px;
            background: #fff;
            border-radius: 10px;
            box-shadow: 0 2px 8px rgba(255,0,0,0.08);
            text-align: center;
        }
        .login-logo {
            width: 100px;
            margin-bottom: 20px;
        }
        .login-title {
            font-size: 24px;
            color: #b30000;
            margin-bottom: 25px;
            font-weight: bold;
        }
        .login-form input[type="email"],
        .login-form input[type="password"] {
            width: 90%;
            padding: 10px;
            margin-bottom: 18px;
            border: 1px solid #ffcccc;
            border-radius: 5px;
            font-size: 16px;
        }
        .login-form button {
            width: 95%;
            padding: 10px;
            background: linear-gradient(to right, #ff4d4d, #ffcccc);
            border: none;
            border-radius: 5px;
            color: white;
            font-size: 18px;
            font-weight: bold;
            cursor: pointer;
            margin-top: 10px;
        }
        .login-form button:hover {
            background: linear-gradient(to right, #ff0000, #ffcccc);
        }
    </style>
</head>
<body>
    <div class="login-container">
        <img src="/logo.png" alt="Logo" class="login-logo">
        <div class="login-title">Login</div>
        <form class="login-form" method="POST" action="/login" autocomplete="off">
            <input type="email" name="email" placeholder="Email" required>
            <input type="password" name="password" placeholder="Password" required>
            <button type="submit">Sign In</button>
            <div style="margin-top: 15px;">
                <a href="/forgot-password" style="color:#b30000; text-decoration:none; font-size:15px;">Forgot password?</a>
            </div>
            <div style="margin-top: 10px;">
                <span style="font-size:15px;">Don't have an account? </span>
                <a href="/signup" style="color:#b30000; text-decoration:none; font-size:15px;">Sign up</a>
            </div>
        </form>
    </div>
</body>
</html>